# Core
MODEL_RUNNER=ollama
BIBLE_PROVIDER=reference-only
ESV_API_KEY=
OUTPUT_FORMAT_DEFAULT=md

# WebUI HTTPS (DNS-first)
LESSON_HOSTNAME=
ACME_EMAIL=
CLOUDFLARE_API_TOKEN=

# Optional override:
# TLS_MODE=dns | mkcert
# CADDY_BIND_IP=0.0.0.0 | 127.0.0.1
