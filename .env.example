# Core
MODEL_RUNNER=ollama
BIBLE_PROVIDER=reference-only
ESV_API_KEY=
OUTPUT_FORMAT_DEFAULT=md

# WebUI HTTPS (DNS-first via Cloudflare)
LESSON_HOSTNAME=lessons.example.com
ACME_EMAIL=you@example.com

# Cloudflare API token with DNS:Edit permission on the zone
CLOUDFLARE_API_TOKEN=

# Optional overrides
# TLS_MODE=dns | mkcert
# CADDY_BIND_IP=0.0.0.0 | 127.0.0.1
